export type FeatureType = {
  title: string;
  description: string;
  example: string;
  icon: string;
  comingSoon?: boolean;
};

export type BenefitType = {
  title: string;
  description: string;
};

export type TargetAudienceType = {
  description: string;
};